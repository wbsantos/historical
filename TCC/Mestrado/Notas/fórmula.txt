Xn=(Sum(X[i], from 1 to n-1)- (Sum(K[i], from 1 to Last(K)) * N/Lenght(K))) *-1
Xn=(Sum(X[i], from 1 to n-1)- (Sum(K[i], from 1 to Last(K)) * N/Lenght(K))) *-1


Xn=(
		Sum(X[i], from 1 to n-1)
		- 
		(
			Sum(K[i], from 1 to Last(K)) 
			* 
			N
			/
			Lenght(K)
		)
	)
	*
	-1


((x1+x2+x3)     -      ((0+1)        *       4/2)    )         *-1
((x1+x2+x3)     -      2   )         *-1


111


Onde K é um conjunto com todos os valores possíveis em um X qualquer
N é a quantidade de X's