#include "classe.h"

classe::classe (int x, int y, int z)
{
  altval(x, y, z);                 
}

void classe::altval(int x, int y, int z) 
{
  altx(x);
  alty(y);
  altz(z);  
}                    
